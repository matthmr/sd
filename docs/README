================================================================
This file contains source code documentation in plain text form.

This is NOT intended to be read by learners of the language
but by developers or anyone who wants to fork or use the
SD API in C.

For anyone learning the language for writing programs or scripts,
read the official language documentation:
	https://matthmr.github.io/docs.sd.

================================================================

Index
-----

1. destack-*
	The program through the <main> stack; targets are `sdread` and `sdc`.

2. functions/next
	The `next` function of `sdparse.o`, responsible for advancing the
	bytestream pointer.

3. data/txt
	The `txt.c` file, responsible for storing all keywords and tokens
	of the SD language.
